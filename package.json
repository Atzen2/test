{
  "name": "@atz3n/actions-npm-playground",
  "version": "1.0.0",
  "description": "just a github actions npm playground",
  "main": "dist/src/index.js",
  "scripts": {
    "test": "npm run test-browser && npm run test-node",
    "test-node": "jest --testEnvironment=node",
    "test-browser": "jest --testEnvironment=jsdom",
    "build": "rm -rf dist && tsc",
    "test-build-node": "jest --rootDir=dist --testEnvironment=node",
    "test-build-browser": "jest --rootDir=dist --testEnvironment=jsdom"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/atz3n/actions-npm-playground.git"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^3.6.4",
    "@types/jest": "^24.0.18",
    "jest": "^24.9.0",
    "jest-cli": "^24.9.0",
    "ts-jest": "^24.1.0"
  },
  "dependencies": {
    "bip39": "^3.0.2"
  }
}
